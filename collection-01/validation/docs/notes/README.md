# `validation/docs/notes/`

The validation lab notebook. Same role and rules as
[`collection-01/docs/notes/`](../../../docs/notes/README.md) — kept here because validation is
**episodic** and its documentation is colocated with its code.

Only one thing belongs here: the record of **routes tried and dropped**, brief enough to read in a
minute, so no one takes them again. Design goes in [`../design.md`](../design.md), which must agree
with the Python; status goes in that document's `Status` section, not here.
