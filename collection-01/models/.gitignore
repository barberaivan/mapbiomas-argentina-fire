# The repo-root .gitignore drops all *.csv. Re-include the small, non-regenerable-cheaply
# model artifacts we DO want versioned (coefficients, metrics, tuning, feasibility)...
!*.csv
# ...but keep the large per-obs predictions and binary fits out of git.
*_oof_predictions.csv
*.rds
