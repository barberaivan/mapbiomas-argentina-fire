# The repo-root .gitignore drops all *.csv and *.rds. Re-include ONLY the coefficient
# exports — the small, terminal deliverable consumed by GEE prediction. Everything else
# heavy (fits, cv_metrics, tuning, oof predictions, prediction_plots) now lives in
# ../models-store/ (the Insync-synced store, symlinked in by setup.sh).
!*_coefficients.csv
